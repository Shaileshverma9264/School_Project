import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "hsl(0, 100%, 100%)" },
    secondary: { main: "hsla(25, 46%, 95%, 1.00)" },
  },
});

export default theme;
